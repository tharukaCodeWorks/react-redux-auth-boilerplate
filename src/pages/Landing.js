import React from 'react';
import Styles from './Landing.module.css';

const Landing = () =>{
    return(
        <div className={Styles.landingContainer}>
            <h1>React Redux Authentication Boilerplate</h1>
        </div>
    );
}

export default Landing;